export default function queryObjectToString(param: object): string {
    let result:string = '';
//     for (const key in param) {
//         if (result !== '')
//             result += '&';
//         result += `${key}=${param[key]}`
//     }
    return result;
}